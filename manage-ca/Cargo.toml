[package]
name = "manage-ca"
version = "0.2.0"
edition = "2021"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
anyhow = "1.0"
dirs = "5.0"
regex = "1.10"

[target.'cfg(target_os = "linux")'.dependencies]
walkdir = "2.5"
